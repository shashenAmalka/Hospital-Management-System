# Navigation Bar & Authentication Flow Diagrams

## 1. Complete System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        Browser Application                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────┐         ┌──────────────┐                      │
│  │  Login Page  │────────▶│ AuthContext  │                      │
│  └──────────────┘         └──────┬───────┘                      │
│                                   │                               │
│                                   │ Dispatches                    │
│                                   │ Events                        │
│                                   ▼                               │
│  ┌──────────────┐         ┌──────────────┐                      │
│  │   Navbar     │◀────────│  Event Bus   │                      │
│  │  Component   │         │  (window)    │                      │
│  └──────┬───────┘         └──────────────┘                      │
│         │                                                         │
│         │ Renders                                                 │
│         ▼                                                         │
│  ┌──────────────────────────────────────┐                       │
│  │  Role-Based Menu Items               │                       │
│  │  - Patient → Dashboard, Appointments │                       │
│  │  - Doctor → Dashboard, Schedule      │                       │
│  │  - Admin → Dashboard, Users          │                       │
│  └──────────────────────────────────────┘                       │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
                          │
                          │ API Calls
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│                         Backend API                              │
│  /api/auth/login                                                 │
│  /api/auth/validate                                              │
│  /api/user/profile                                               │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. Login Flow Diagram

```
USER                  LOGIN PAGE           AUTH CONTEXT            NAVBAR
 │                         │                      │                   │
 │  Enters Credentials     │                      │                   │
 ├────────────────────────▶│                      │                   │
 │                         │                      │                   │
 │                         │  login(credentials)  │                   │
 │                         ├─────────────────────▶│                   │
 │                         │                      │                   │
 │                         │                      │  API Call         │
 │                         │                      ├──────────────────▶│
 │                         │                      │                   │
 │                         │                      │◀─────────────────┤
 │                         │                      │  {user, token}    │
 │                         │                      │                   │
 │                         │                      │  Store in         │
 │                         │                      │  localStorage     │
 │                         │                      │                   │
 │                         │                      │  Dispatch Event   │
 │                         │                      │  'auth-state-     │
 │                         │                      │   change'         │
 │                         │                      ├───────────────────▶
 │                         │                      │                   │
 │                         │  {success, user}     │                   │
 │                         │◀─────────────────────┤                   │
 │                         │                      │                   │
 │  Redirect to Dashboard  │                      │     Update UI     │
 │◀────────────────────────┤                      │                   │
 │                         │                      │    Show Profile   │
 │                         │                      │    Show Menus     │
 │                         │                      │◀──────────────────┤
 │                         │                      │                   │
 │      See Dashboard      │                      │                   │
 │      with Updated       │                      │                   │
 │      Navigation Bar     │                      │                   │
 │◀────────────────────────┴──────────────────────┴───────────────────┤
 │                                                                     │
 │                    🎉 LOGIN COMPLETE 🎉                            │
 │                    NO PAGE REFRESH!                                │
 └─────────────────────────────────────────────────────────────────────┘
```

---

## 3. Real-Time Update Mechanism

```
┌─────────────────────────────────────────────────────────────────────┐
│                         Event-Driven Updates                         │
└─────────────────────────────────────────────────────────────────────┘

AuthContext (Publisher)                      Navbar (Subscriber)
     │                                             │
     │  User logs in                               │
     ├─────────────────────────────────────────────┤
     │                                             │
     │  1. Update internal state                   │
     │     - setUser(userData)                     │
     │     - setIsAuthenticated(true)              │
     │                                             │
     │  2. Store in localStorage                   │
     │     - localStorage.setItem('user', ...)     │
     │     - localStorage.setItem('token', ...)    │
     │                                             │
     │  3. Dispatch custom event                   │
     │     window.dispatchEvent(                   │
     │       'auth-state-change',                  │
     │       { isAuthenticated, user }             │
     │     )                                       │
     ├────────────────────────────────────────────▶│
     │                                             │
     │                                   4. Event listener triggered
     │                                             │
     │                                   5. Update local state
     │                                      - setAuthState({
     │                                          isAuthenticated,
     │                                          user
     │                                        })
     │                                             │
     │                                   6. Component re-renders
     │                                      - Show user profile
     │                                      - Show role menus
     │                                      - Update buttons
     │                                             │
     │                         ✨ UI UPDATED INSTANTLY ✨
     │                         NO PAGE REFRESH NEEDED!
     │                                             │
```

---

## 4. Role-Based Menu Rendering

```
┌──────────────────────────────────────────────────────────────────┐
│                    getRoleBasedMenuItems()                        │
└──────────────────────────────────────────────────────────────────┘

Input: user.role = "patient"
          │
          ▼
    ┌─────────┐
    │ Switch  │
    │  Role   │
    └────┬────┘
         │
         ├──▶ patient ──────┐
         │                  │
         ├──▶ doctor        │
         │                  │
         ├──▶ admin         │
         │                  │
         ├──▶ lab_tech      │
         │                  │
         └──▶ pharmacist    │
                            │
                            ▼
              ┌─────────────────────────────┐
              │    Return Menu Items        │
              │                             │
              │  [                          │
              │    Dashboard,               │
              │    Appointments,            │
              │    Lab Reports              │
              │  ]                          │
              └─────────────┬───────────────┘
                            │
                            ▼
              ┌─────────────────────────────┐
              │   Navbar renders items      │
              │                             │
              │   [Dashboard] [Appointments]│
              │   [Lab Reports]             │
              └─────────────────────────────┘
```

---

## 5. Logout Flow

```
USER              NAVBAR DROPDOWN       AUTH CONTEXT         NAVBAR
 │                      │                     │                 │
 │  Clicks Logout       │                     │                 │
 ├─────────────────────▶│                     │                 │
 │                      │                     │                 │
 │                      │  logout()           │                 │
 │                      ├────────────────────▶│                 │
 │                      │                     │                 │
 │                      │              1. Clear state           │
 │                      │                 - setUser(null)       │
 │                      │                 - setToken(null)      │
 │                      │                 - setIsAuth(false)    │
 │                      │                     │                 │
 │                      │              2. Clear localStorage    │
 │                      │                 - removeItem('user')  │
 │                      │                 - removeItem('token') │
 │                      │                     │                 │
 │                      │              3. Dispatch events       │
 │                      │                 - 'auth-state-change' │
 │                      │                 - 'logout'            │
 │                      │                     ├─────────────────▶
 │                      │                     │                 │
 │                      │                     │      4. Update UI
 │                      │                     │         - Hide profile
 │                      │                     │         - Show public menu
 │                      │                     │         - Show Login btn
 │                      │                     │                 │
 │  Redirect to /login  │                     │                 │
 │◀─────────────────────┴─────────────────────┴─────────────────┤
 │                                                               │
 │               🔓 LOGOUT COMPLETE 🔓                          │
 │               SEE PUBLIC NAVIGATION                           │
 └───────────────────────────────────────────────────────────────┘
```

---

## 6. Component State Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                    Navbar Component States                       │
└─────────────────────────────────────────────────────────────────┘

                    ┌──────────────────┐
                    │  Initial State   │
                    │  isAuth: false   │
                    │  user: null      │
                    └────────┬─────────┘
                             │
                             │ User logs in
                             │ Event received
                             ▼
                    ┌──────────────────┐
                    │ Authenticated    │
                    │ isAuth: true     │
                    │ user: {...}      │
                    └────────┬─────────┘
                             │
                    ┌────────┴────────┐
                    │                 │
         Role:      ▼                 ▼
              ┌──────────┐      ┌──────────┐
              │ Patient  │      │  Doctor  │      ...more roles
              │ Menus    │      │  Menus   │
              └──────────┘      └──────────┘
                    │                 │
                    └────────┬────────┘
                             │
                             │ User logs out
                             │ Event received
                             ▼
                    ┌──────────────────┐
                    │ Unauthenticated  │
                    │ isAuth: false    │
                    │ user: null       │
                    └──────────────────┘
                             │
                             │ Loop back
                             ▼
                    ┌──────────────────┐
                    │  Initial State   │
                    └──────────────────┘
```

---

## 7. Mobile vs Desktop Navigation

```
┌─────────────────────────────────────────────────────────────────┐
│                    Responsive Breakpoint                         │
│                     < 768px = Mobile                             │
│                     ≥ 768px = Desktop                            │
└─────────────────────────────────────────────────────────────────┘

DESKTOP (≥ 768px)                    MOBILE (< 768px)
┌───────────────────────────────┐   ┌─────────────────────────┐
│ HelaMed | Dashboard | Profile │   │ HelaMed         [☰]    │
│         | Appointments | [🔔] │   └─────────────────────────┘
│         | [👤 John Doe ▼]     │                │
└───────────────────────────────┘                │ Tap hamburger
         │ Click avatar                          ▼
         ▼                              ┌─────────────────────────┐
┌───────────────────────┐              │ 👤 John Doe             │
│ 👤 John Doe           │              │ Patient                 │
│ john@email.com        │              │─────────────────────────│
│───────────────────────│              │ ▶ Dashboard             │
│ ▶ Dashboard           │              │ ▶ Appointments          │
│ ▶ My Profile          │              │ ▶ Lab Reports           │
│───────────────────────│              │ ▶ My Profile            │
│ ▶ Logout              │              │─────────────────────────│
└───────────────────────┘              │ ▶ Logout                │
                                       └─────────────────────────┘
    Dropdown Menu                       Full-Screen Drawer
```

---

## 8. Data Flow Summary

```
┌────────────────────────────────────────────────────────────────────┐
│                        Complete Data Flow                           │
└────────────────────────────────────────────────────────────────────┘

1. LOGIN
   Form → AuthContext → API → Response
                            ↓
   localStorage ← Store ← Success
                            ↓
   Event Dispatch → 'auth-state-change'
                            ↓
   Navbar listens ← Update UI
                            ↓
   Navigate ← Redirect to Dashboard

2. NAVIGATION
   Click Link → React Router → New Page
                                  ↓
   Navbar stays ← Consistent across pages
                                  ↓
   Active highlight ← Current route

3. LOGOUT
   Click Logout → AuthContext → Clear data
                                    ↓
   localStorage ← Remove items
                                    ↓
   Event Dispatch → 'logout'
                                    ↓
   Navbar listens ← Reset UI
                                    ↓
   Navigate ← Redirect to Login

4. REAL-TIME UPDATE
   Any auth change → Event dispatched
                                  ↓
   All listeners notified
                                  ↓
   UI updates everywhere instantly
```

---

## 9. Security Flow

```
┌────────────────────────────────────────────────────────────────────┐
│                       Security Measures                             │
└────────────────────────────────────────────────────────────────────┘

1. TOKEN STORAGE
   Login Success
        ↓
   Store token in localStorage (encrypted)
        ↓
   Include in all API requests
        ↓
   Validate on every request

2. TOKEN VALIDATION
   App Loads
        ↓
   Check localStorage for token
        ↓
   If exists → Validate with API
        ↓
   If valid → Load user data
        ↓
   If invalid → Clear and redirect to login

3. AUTO-LOGOUT
   API returns 401 Unauthorized
        ↓
   Dispatch 'auth-error' event
        ↓
   AuthContext catches event
        ↓
   Clear all auth data
        ↓
   Redirect to login
        ↓
   Show "Session expired" message

4. PROTECTED ROUTES
   User navigates to protected page
        ↓
   Check isAuthenticated
        ↓
   If true → Allow access
        ↓
   If false → Redirect to login
        ↓
   Store intended destination
        ↓
   After login → Redirect back
```

---

## 10. Performance Optimization

```
┌────────────────────────────────────────────────────────────────────┐
│                    Performance Strategy                             │
└────────────────────────────────────────────────────────────────────┘

1. EVENT LISTENERS
   Component Mount
        ↓
   Add event listeners
        ↓
   Component Unmount
        ↓
   Remove event listeners ← Prevent memory leaks

2. STATE MANAGEMENT
   Minimal re-renders
        ↓
   Proper dependencies in useEffect
        ↓
   Memoized callbacks
        ↓
   Optimized state updates

3. CACHING
   User data → localStorage
        ↓
   Avoid repeated API calls
        ↓
   Load from cache on mount
        ↓
   Validate when needed

4. CODE SPLITTING
   Lazy load dashboard components
        ↓
   Reduce initial bundle size
        ↓
   Faster initial page load
        ↓
   Load routes on demand
```

---

## Visual Legend

```
┌────────────────────────────────────────┐
│ Symbol Key                             │
├────────────────────────────────────────┤
│ ──▶  : Data flow                       │
│ ◀──  : Return value                    │
│ ├──▶ : Branch/Option                   │
│ │    : Sequential step                 │
│ ▼    : Next step                       │
│ ┌──┐ : Component/Module                │
│ [  ] : UI Element                      │
│ {  } : Data object                     │
│ ✨   : Highlight/Success                │
│ 🎉   : Achievement                      │
│ 🔓   : Security action                  │
└────────────────────────────────────────┘
```

---

## Quick Reference Chart

```
┌──────────────┬────────────────────┬──────────────────────┐
│   Action     │   Triggers         │      Result          │
├──────────────┼────────────────────┼──────────────────────┤
│ Login        │ auth-state-change  │ Nav shows profile    │
│ Logout       │ logout, auth-...   │ Nav shows public     │
│ Page Load    │ localStorage check │ Auto-login if valid  │
│ API Error    │ auth-error event   │ Auto-logout          │
│ Route Change │ React Router       │ Highlight active     │
│ Role Change  │ User object update │ Different menus      │
└──────────────┴────────────────────┴──────────────────────┘
```

---

**These diagrams show how all the pieces work together to create a seamless, real-time navigation experience!** 🚀