# 📊 Patient History Tab - System Flow Diagram

## 🔄 Complete Data Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                     PATIENT USER                                │
│  (Logged in with JWT token in localStorage)                    │
└───────────────────────┬─────────────────────────────────────────┘
                        │
                        │ Clicks "Visit History" tab
                        ↓
┌─────────────────────────────────────────────────────────────────┐
│                  HISTORY TAB COMPONENT                          │
│  (HistoryTab.jsx)                                              │
└───────────────────────┬─────────────────────────────────────────┘
                        │
                        │ useEffect triggers
                        │ fetchHistoryData()
                        ↓
┌─────────────────────────────────────────────────────────────────┐
│                 PARALLEL API CALLS                              │
│                                                                 │
│  ┌──────────────────────┐   ┌───────────────────────┐         │
│  │  API Call #1         │   │  API Call #2          │         │
│  │  GET /appointments   │   │  GET /lab-reports     │         │
│  │  /user/:userId       │   │  ?patientId=:id       │         │
│  └──────────────────────┘   └───────────────────────┘         │
└───────────┬──────────────────────────┬──────────────────────────┘
            │                          │
            │ Authorization:           │ Authorization:
            │ Bearer <token>           │ Bearer <token>
            ↓                          ↓
┌─────────────────────────────────────────────────────────────────┐
│                    BACKEND SERVER                               │
│                (Port 5000)                                      │
│                                                                 │
│  ┌──────────────────────┐   ┌───────────────────────┐         │
│  │ Appointment          │   │ Lab Report            │         │
│  │ Controller           │   │ Controller            │         │
│  └──────────────────────┘   └───────────────────────┘         │
└───────────┬──────────────────────────┬──────────────────────────┘
            │                          │
            │ Query MongoDB            │ Query MongoDB
            ↓                          ↓
┌─────────────────────────────────────────────────────────────────┐
│                    MONGODB DATABASE                             │
│                                                                 │
│  ┌──────────────────────┐   ┌───────────────────────┐         │
│  │ appointments         │   │ lab_reports           │         │
│  │ collection           │   │ collection            │         │
│  └──────────────────────┘   └───────────────────────┘         │
└───────────┬──────────────────────────┬──────────────────────────┘
            │                          │
            │ Returns data             │ Returns data
            ↓                          ↓
┌─────────────────────────────────────────────────────────────────┐
│              RESPONSE DATA (JSON)                               │
│                                                                 │
│  ┌──────────────────────┐   ┌───────────────────────┐         │
│  │ [{                   │   │ [{                    │         │
│  │   _id: "...",        │   │   _id: "...",         │         │
│  │   appointmentType,   │   │   testType: "...",    │         │
│  │   doctorName,        │   │   result: "...",      │         │
│  │   status,            │   │   status,             │         │
│  │   appointmentDate    │   │   reportDate          │         │
│  │ }]                   │   │ }]                    │         │
│  └──────────────────────┘   └───────────────────────┘         │
└───────────┬──────────────────────────┬──────────────────────────┘
            │                          │
            │ setAppointments()        │ setLabReports()
            └──────────┬───────────────┘
                       │
                       ↓
┌─────────────────────────────────────────────────────────────────┐
│              COMPONENT STATE UPDATED                            │
│                                                                 │
│  appointments: [...]  labReports: [...]  loading: false        │
└───────────────────────┬─────────────────────────────────────────┘
                        │
                        │ Re-render
                        ↓
┌─────────────────────────────────────────────────────────────────┐
│                  UI RENDERING                                   │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    HEADER                                │  │
│  │  Visit History                    [All|Appointments|Labs]│  │
│  │  X records • Y appointments • Z lab reports              │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              APPOINTMENTS SECTION                        │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ 📅 General Consultation          [Completed ✅]    │  │  │
│  │  │    Dr. Silva • Oct 15, 2025                        │  │  │
│  │  │                                          [▼ Expand] │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              LAB REPORTS SECTION                         │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ 🧪 Blood Test                    [Completed ✅]    │  │  │
│  │  │    Lab Tech: John • Oct 14, 2025                  │  │  │
│  │  │                                          [▼ Expand] │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                        │
                        │ User clicks expand
                        ↓
┌─────────────────────────────────────────────────────────────────┐
│              EXPANDED CARD VIEW                                 │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ 🧪 Blood Test                        [Completed ✅]        │ │
│  │    Lab Tech: John • Oct 14, 2025                          │ │
│  │                                              [▲ Collapse]  │ │
│  ├────────────────────────────────────────────────────────────┤ │
│  │ ✅ Result: Normal                                         │ │
│  │ 📋 Notes: All values within normal range                  │ │
│  │ ⚠️ [Critical Result Warning] (if isCritical = true)       │ │
│  │ [📥 Download Report]  [👁️ View Report]                    │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔐 Authentication Flow

```
┌─────────────────┐
│  User Login     │
│  (Login.jsx)    │
└────────┬────────┘
         │
         │ Credentials valid
         ↓
┌─────────────────────────────────┐
│  Backend validates              │
│  & generates JWT token          │
└────────┬────────────────────────┘
         │
         │ Returns token + user data
         ↓
┌─────────────────────────────────┐
│  Store in localStorage:         │
│  - token: "eyJ..."              │
│  - user: {_id, role, email...}  │
└────────┬────────────────────────┘
         │
         │ Navigate to dashboard
         ↓
┌─────────────────────────────────┐
│  Patient Dashboard loads        │
│  with user data                 │
└────────┬────────────────────────┘
         │
         │ Click "Visit History"
         ↓
┌─────────────────────────────────┐
│  HistoryTab reads:              │
│  - user._id from props          │
│  - token from localStorage      │
└────────┬────────────────────────┘
         │
         │ Makes authenticated API calls
         ↓
┌─────────────────────────────────┐
│  Backend validates token        │
│  & returns user-specific data   │
└─────────────────────────────────┘
```

---

## 🎨 Component State Management

```
┌──────────────────────────────────────────────┐
│         HistoryTab Component State           │
├──────────────────────────────────────────────┤
│                                              │
│  appointments: []                            │
│    ↓                                         │
│    Stores array of appointment objects       │
│                                              │
│  labReports: []                              │
│    ↓                                         │
│    Stores array of lab report objects        │
│                                              │
│  loading: true/false                         │
│    ↓                                         │
│    Controls spinner visibility               │
│                                              │
│  error: null/"error message"                 │
│    ↓                                         │
│    Stores error for display                  │
│                                              │
│  activeView: 'all'|'appointments'|'labs'     │
│    ↓                                         │
│    Controls filter tabs                      │
│                                              │
│  expandedItems: { "apt-123": true, ... }     │
│    ↓                                         │
│    Tracks which cards are expanded           │
│                                              │
└──────────────────────────────────────────────┘
```

---

## 🔄 Card Expansion Logic

```
User clicks chevron (▼)
         │
         ↓
┌────────────────────────────────┐
│  toggleExpand(id) called       │
└────────┬───────────────────────┘
         │
         ↓
┌────────────────────────────────┐
│  Check expandedItems state:    │
│  expandedItems["apt-123"]      │
└────────┬───────────────────────┘
         │
         ├─ If true (expanded)  → Set to false (collapse)
         │
         └─ If false (collapsed) → Set to true (expand)
         │
         ↓
┌────────────────────────────────┐
│  Component re-renders          │
│  with updated state            │
└────────┬───────────────────────┘
         │
         ├─ If expanded: Show details section
         │
         └─ If collapsed: Hide details section
         │
         ↓
┌────────────────────────────────┐
│  Chevron icon changes:         │
│  ▼ (down) ↔ ▲ (up)            │
└────────────────────────────────┘
```

---

## 📊 Filter Logic Flow

```
User clicks filter button
         │
         ├─ "All"
         │   └─ setActiveView('all')
         │       └─ Show: appointments + labReports
         │
         ├─ "Appointments"
         │   └─ setActiveView('appointments')
         │       └─ Show: appointments only
         │
         └─ "Lab Reports"
             └─ setActiveView('labs')
                 └─ Show: labReports only
         │
         ↓
Component re-renders with filtered data
         │
         ↓
Conditional rendering based on activeView
         │
         ├─ if (activeView === 'all' || activeView === 'appointments')
         │   └─ Render appointments section
         │
         └─ if (activeView === 'all' || activeView === 'labs')
             └─ Render lab reports section
```

---

## 🎯 Data Filtering

```
┌──────────────────────────────────────┐
│  Raw Appointments from Backend       │
│  [{status: "completed"}, ...]        │
└────────┬─────────────────────────────┘
         │
         ↓
┌──────────────────────────────────────┐
│  Filter for completed/past only:     │
│                                      │
│  appointments.filter(apt =>          │
│    apt.status === 'completed' ||     │
│    apt.status === 'confirmed' ||     │
│    new Date(apt.date) < new Date()   │
│  )                                   │
└────────┬─────────────────────────────┘
         │
         ↓
┌──────────────────────────────────────┐
│  Filtered Appointments                │
│  [{status: "completed"}, ...]        │
│  (Only past/completed ones)          │
└──────────────────────────────────────┘
```

---

## ⚠️ Error Handling Flow

```
API Call Made
     │
     ├─ Success (200 OK)
     │   └─ Data received
     │       └─ Update state
     │           └─ Render cards
     │
     ├─ Error (401, 404, 500, etc.)
     │   └─ Catch error
     │       └─ setError(message)
     │           └─ Show error UI
     │               └─ Display "Try Again" button
     │
     └─ Network Error
         └─ Catch error
             └─ setError("Network error")
                 └─ Show error UI
```

---

## 🎨 UI State Machine

```
                  ┌─────────────┐
                  │   INITIAL   │
                  │ loading=true│
                  └──────┬──────┘
                         │
                         │ API calls start
                         │
          ┌──────────────┴───────────────┐
          │                              │
     SUCCESS                          ERROR
          │                              │
          ↓                              ↓
   ┌──────────────┐              ┌──────────────┐
   │   LOADED     │              │    ERROR     │
   │ loading=false│              │ error=message│
   │ data present │              │ loading=false│
   └──────┬───────┘              └──────┬───────┘
          │                              │
          │                              │ User clicks "Try Again"
          ↓                              │
   ┌──────────────┐                     │
   │  RENDERING   │◄────────────────────┘
   │  Show cards  │
   └──────────────┘
          │
          │ User interaction
          ↓
   ┌──────────────┐
   │  EXPANDED    │
   │  Show details│
   └──────────────┘
```

---

## 📱 Responsive Design Flow

```
Screen Width Check
     │
     ├─ Desktop (≥1024px)
     │   ├─ Full width cards
     │   ├─ Side-by-side buttons
     │   └─ Multiple columns
     │
     ├─ Tablet (768px - 1023px)
     │   ├─ Stacked cards
     │   ├─ Full width components
     │   └─ Horizontal buttons
     │
     └─ Mobile (<768px)
         ├─ Vertical stack
         ├─ Compact spacing
         ├─ Touch-friendly buttons
         └─ Scrollable content
```

---

## 🔄 Complete User Journey

```
1. User Login
   └─ Credentials validated
       └─ JWT token generated
           └─ Stored in localStorage

2. Navigate to Dashboard
   └─ Patient Dashboard loads
       └─ User data displayed
           └─ Tabs visible

3. Click "Visit History"
   └─ HistoryTab component mounts
       └─ useEffect triggers
           └─ fetchHistoryData() called

4. Parallel API Calls
   ├─ Fetch appointments
   └─ Fetch lab reports

5. Data Processing
   ├─ Filter completed appointments
   ├─ Store in state
   └─ Remove loading spinner

6. UI Rendering
   ├─ Show header with counts
   ├─ Render appointment cards
   └─ Render lab report cards

7. User Interactions
   ├─ Click filters → Filter data
   ├─ Click expand → Show details
   └─ Click download → Download report

8. Error Handling (if needed)
   └─ Show error message
       └─ Provide "Try Again" button
           └─ Retry data fetch
```

---

**සම්පූර්ණයි! 🎉**

මේ diagram එකෙන් system එක කොහොමද work කරන්නේ කියලා හරියටම පේන්න ඕන!
